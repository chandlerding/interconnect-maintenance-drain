# Project-Scoped Behavioral Rules

- **Git Commits**: NEVER propose or execute a `git commit` command automatically after modifying code or text files, unless the user explicitly instructs you to do so. All code changes should remain staged or in the working directory for user review.
